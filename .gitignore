node_modules/
dist/
coverage/
.turbo/
*.tsbuildinfo

# model weights are downloaded and cached at runtime, never committed
.models/
*.onnx

.DS_Store
*.log
.env
.env.*
!.env.example

# throwaway spikes and experiments; never part of the build
trial/

# local planning and specs, never published
planning/
